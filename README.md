# python-script-for-automating-deleting-of-files
This is a simple python Script for deleting files from a windows machine.

To run this scripts 
   -install python3
   - Then the script inside the python command line
